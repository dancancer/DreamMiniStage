**一旦我所属的文件夹有所变化，请更新我**

# api/

API 层。当前只保留模型后端统一接口；用户身份走本地 guest 状态，不在这里保留 dormant 账号 API。

## 文件清单

| 文件 | 地位 | 功能 |
|------|------|------|
| `index.ts` | 模块入口 | 导出 API 功能 |
| `backends.ts` | 核心 | 后端服务配置 |
